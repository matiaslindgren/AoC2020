on:
  push:
    branches: main

jobs:
  run:
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: '1.15'
      - run: make
      - run: ./run.sh
